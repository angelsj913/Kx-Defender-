# API status

Kx-Defender does not expose a web API. The supported interface is the local `kx` command.

See [KxLang](kxlang.md) for command grammar and [KX commands](KX-COMMANDS.md) for examples. Interactive commands use human-readable output; one-shot commands retain structured JSON for automation.
